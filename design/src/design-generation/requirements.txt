# Зависимости проекта CONTUR (Python 3.10+)
# Установка:  python -m pip install -r requirements.txt

# GUI
PySide6>=6.6

# Работа с PDF (импортируется как fitz)
PyMuPDF>=1.24

# Выполнение и разбор Lua
lupa>=2.0

# Детекция устройств на схеме
ultralytics>=8.3
opencv-python>=4.9
numpy>=1.26

# Генерация SVG
svgwrite>=1.4

# Экспорт в PostgreSQL
psycopg2-binary>=2.9

# Сборка исполняемого файла (необязательно)
# pyinstaller>=6.0
