# main.py
# Точка входа приложения.
#
# Запуск:
#     python main.py
#
# Само окно и вся его работа — в contur/ui/main_window.py; здесь только
# запуск, чтобы точка входа не пряталась внутри слоя и её было видно
# в корне модуля. Сборка .exe собирается от этого же файла
# (МоеПриложение.spec).
from contur.ui.main_window import main

if __name__ == "__main__":
    main()
