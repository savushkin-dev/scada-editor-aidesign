"""Формат редактора мнемосхем — отдельный контракт со своей моделью.

Остальные выгрузки (`contur/export`) — три записи одного и того же
PlantGeometry 1.3: XML, JSON и таблицы PostgreSQL. Здесь модель другая:
плоский массив элементов холста, сетка 20, готовые фигуры библиотеки
и размер листа блоком `canvas`. Отдельная спецификация с нумерованными
разделами, свой каталог фигур (`hmi_symbols.json`) и своя приёмка.

`exporter` собирает файл, `symbols` держит каталог готовых фигур и выбирает
их по типу устройства, `validate` проверяет готовый файл по спецификации.
"""
