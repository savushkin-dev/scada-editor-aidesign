"""Генеративный дизайн мнемосхем: чертёж Eplan и описание контроллера
сводятся в машинно-читаемое описание установки.

Слои идут по ходу конвейера: `pdf` разбирает чертёж, `lua` — описание
контроллера, `matching` связывает одно с другим, `export` отдаёт
результат, `ui` показывает его в окне. `core` — общий фундамент,
он ни от кого не зависит.
"""
